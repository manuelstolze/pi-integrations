---
status: proposed
---

# Add optional branch creation to Herald

Herald currently requires the user to select the current branch and does not create or switch branches. A future design may add user-approved branch creation, but it must first define branch naming, base branch selection, existing-branch handling, switching, and recovery rules.
