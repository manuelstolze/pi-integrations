---
status: accepted
---

# Require repository contribution policy for Herald

Herald requires a readable `CONTRIBUTING.md` before it plans commits or requests. It does not use a fallback commit policy when the file is missing, while provider request templates remain optional; this makes the repository define its own commit and validation rules before Herald performs permanent work.
